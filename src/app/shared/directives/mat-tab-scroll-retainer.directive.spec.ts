import { MatTabScrollRetainerDirective } from './mat-tab-scroll-retainer.directive';

describe('MatTabScrollRetainerDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTabScrollRetainerDirective();
    expect(directive).toBeTruthy();
  });
});
